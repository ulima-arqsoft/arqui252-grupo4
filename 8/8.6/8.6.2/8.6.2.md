> [8. Aplicación de Patrones Arquitecturales](../../8.md) › [8.6. Listado Consolidado](../8.6.md) › [8.6.2. Patrones de Componente y Conector](8.6.2.md)

# 8.6.2. Patrones de Componente y Conector

## **03: Módulo: Carrito de Compras y Pedidos**

---

**Título:**
Patrón de Persistencia Híbrida para Carritos y Pedidos

**Contexto:**
El Módulo 3: Carrito de Compras y Pedidos (CCP) maneja dos tipos de datos con necesidades opuestas. El **carrito de compras** es temporal, volátil y requiere acceso de muy alta velocidad para no afectar la experiencia del usuario. Por otro lado, los **pedidos** son permanentes, transaccionales y la "fuente de verdad" de una venta completada. Se necesita una estrategia de persistencia que optimice el rendimiento para los carritos sin sacrificar la consistencia transaccional de los pedidos.

**Alternativas:**

* **Patrón de Datos Compartidos (Híbrido: Redis + SQL):**
    * Usa `Redis` para los carritos (en memoria) y `PostgreSQL` para los pedidos (en disco).
    * Ventaja: Alto rendimiento del carrito y fuerte consistencia (ACID) para pedidos.
    * Ventaja: Las operaciones del carrito no sobrecargan la BD transaccional.
    * Desventaja: Requiere mantener y conectar dos sistemas de BD.

* **Patrón de Datos Compartidos (Relacional Único):**
    * Usa `PostgreSQL` para almacenar *tanto* carritos como pedidos.
    * Ventaja: Simplicidad (una sola tecnología de BD).
    * Desventaja: Bajo rendimiento del carrito (cada "añadir" es una escritura en disco).
    * Desventaja: La BD de pedidos se "ensucia" con datos temporales de carritos abandonados.

**Criterios de elección:**

* **Rendimiento del Carrito:** La UI debe ser instantánea al añadir/quitar ítems (`RF-01`).
* **Consistencia Transaccional:** Los pedidos completados (ventas) deben ser 100% fiables (ACID).
* **Desacoplamiento de Carga:** Las operaciones del carrito (alta frecuencia) no deben afectar el rendimiento de la BD de pedidos (alta criticidad).

**Decisión:**
Se adopta el **Patrón de Datos Compartidos Híbrido (Redis + SQL)**.

**Sustento:**
Esta decisión es obligatoria por las restricciones del proyecto, que indican explícitamente el uso de `Redis` para el carrito y `PostgreSQL` para los pedidos. Es la única alternativa que cumple con los requisitos de alto rendimiento (`RF-01`) sin sacrificar la consistencia.

El `Servicio de Carrito y Pedidos (CCP)` implementa esta separación, donde el `CartRepository` escribe en `Redis` y el `OrderRepository` escribe en `PostgreSQL`

## **04: Módulo: Sistema de Pagos**

--

**Título:**
Uso de Webhooks Asíncronos (Patrón Publicar-Suscribir) para Confirmación de Pagos

**Contexto:**
El Módulo 4 (SP) debe recibir la confirmación de pago del proveedor externo Culqi. El sistema debe ser 100% fiable, ya que esta confirmación es la que autoriza la creación de un pedido en el Módulo 3. Se debe definir el mecanismo de comunicación (conector) para recibir esta confirmación.

**Alternativas:**

* **Webhooks Asíncronos (Patrón Publicar-Suscribir):**
    * Culqi (Publicador) envía un evento (HTTP POST) a un endpoint nuestro (Suscriptor) cuando el pago se completa.
    * Ventaja: Alta fiabilidad; garantiza la entrega incluso si el usuario cierra el navegador.
    * Ventaja: Desacoplado; el sistema reacciona a eventos en lugar de gastar recursos esperando.
    * Desventaja: Requiere un endpoint público seguro para recibir el evento.

* **Polling Síncrono (Patrón Solicitud-Respuesta):**
    * Nuestro Servicio de Pagos (SP) pregunta a la API de Culqi repetidamente ("polling") si el pago ya se realizó.
    * Ventaja: Flujo de código aparentemente más simple.
    * Desventaja: Muy frágil; falla si el usuario cierra la ventana, dejando el pedido en el limbo.
    * Desventaja: Ineficiente (genera tráfico de red innecesario) y prohibido por la mayoría de las pasarelas.

**Criterios de elección:**

* **Fiabilidad:** Garantizar la captura del 100% de los pagos (RF-03).
* **Desacoplamiento:** No bloquear el Servicio de Pagos (SP) esperando una respuesta síncrona.
* **Cumplimiento de Proveedor:** Seguir las mejores prácticas y requisitos obligatorios del proveedor (Culqi).

**Decisión:**
Se adopta el patrón de Webhooks Asíncronos (Publicar-Suscribir).

**Sustento:**
La decisión es obligatoria según la restricción del proyecto: "Las notificaciones de transacciones [...] deben ser gestionadas a través de los webhooks proporcionados por cada pasarela de pago". Este patrón es el único que cumple el requisito de fiabilidad RF-03.

---
## **05: Módulo: Comunidad y Reseñas**

---

**Título:**
Coreografía Asíncrona (Patrón Publicar-Suscribir) para Notificación de Reseñas

**Contexto:**
El `Servicio de Comunidad y Reseñas (SCR)` es el propietario de los datos de las reseñas. Cuando se crea una nueva reseña, otros módulos necesitan ser informados para reaccionar. Por ejemplo, el `Servicio de Catálogo` (Módulo 02) debe recalcular el puntaje promedio del videojuego. Se debe definir el conector para comunicar este evento sin crear dependencias críticas.

**Alternativas:**

* **Coreografía Asíncrona (Patrón Publicar-Suscribir):**
    * El `SCR` (Módulo 05) guarda la reseña en su BD e inmediatamente publica un evento (`NUEVA_RESEÑA_CREADA`) en un bus de eventos (ej. RabbitMQ).
    * Ventaja: Rápido para el usuario (respuesta de API instantánea).
    * Ventaja: Desacoplamiento total. El `SCR` no sabe ni le importa quién lo escucha. Se pueden añadir nuevos suscriptores (ej. un servicio de "Logros") sin tocar el Módulo 05.
    * Desventaja: Requiere infraestructura de mensajería (bus de eventos).

* **Orquestación Síncrona (Patrón Solicitud-Respuesta / API REST):**
    * El `SCR` (Módulo 05), *después* de guardar la reseña, realiza llamadas HTTP `POST` directas al `Servicio de Catálogo`.
    * Ventaja: Flujo de lógica simple y explícito dentro del código del Módulo 05.
    * Desventaja: Pésimo rendimiento; el usuario espera a que los servicios terminen.
    * Desventaja: Alto acoplamiento.

**Criterios de elección:**

* **Rendimiento (Experiencia de Usuario):** El tiempo de respuesta del `POST /reseñas` debe ser mínimo.
* **Desacoplamiento y Extensibilidad:** Facilidad para añadir nuevos servicios que reaccionen a una reseña sin modificar el `SCR`.
* **Resiliencia:** El fallo de un servicio secundario (como Notificaciones) no debe afectar la funcionalidad principal (crear la reseña).

**Decisión:**
Se adopta la **Coreografía Asíncrona (Patrón Publicar-Suscribir)**.

**Sustento:**
La **Orquestación Síncrona** se descarta por ser inviable, ya que viola los criterios de rendimiento y resiliencia; acopla el destino del Módulo 05 al de servicios menos críticos. El patrón **Publicar-Suscribir** es la única opción que garantiza una respuesta de API instantánea para el usuario. Permite que el `SCR` cumpla su única responsabilidad (gestionar reseñas) de forma eficiente y se desacople de las responsabilidades de otros servicios (recalcular puntajes, enviar correos), lo cual es fundamental para una arquitectura de microservicios robusta.

---
[⬅️ Anterior](../8.6.1/8.6.1.md) | [🏠 Home](../../../README.md) | [Siguiente ➡️](../8.6.3/8.6.3.md)
