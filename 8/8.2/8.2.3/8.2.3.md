8. Aplicación de Patrones Arquitecturales
 › 8.2. Módulo 2 / Integrante 2
 › 8.2.3. Patrones de Asignación

8.2.3. Patrones de Asignación
02: Módulo: Gestión de Usuarios

Título:
Asignación del Servicio de Gestión de Usuarios (SGU) como Microservicio en Contenedor Independiente

Contexto:
El Servicio de Gestión de Usuarios procesa operaciones críticas para la plataforma, como autenticación, registro y validación de roles. Una falta de disponibilidad en este servicio podría bloquear el acceso a todo el ecosistema. Es necesario seleccionar el patrón de asignación que garantice seguridad, disponibilidad y escalabilidad de forma independiente al resto de módulos.

Alternativas:

Patrón de Microservicio (Asignación Independiente en Contenedor):

El SGU se despliega como un servicio completamente independiente, aislado en su propio contenedor/pod.

Permite escalar autenticación sin escalar el resto del sistema.

Aísla fallos: una caída en otro módulo no afecta el login ni el registro.

Patrón Multi-Tier (N-Capas):

El servicio se despliega junto con otros microservicios en un mismo tier o contenedor.

Reduce complejidad inicial pero sacrifica resiliencia y escalabilidad independiente.

Patrón Monolítico (Unidad Única):

El SGU se incluye dentro de una aplicación monolítica mayor.

Simplicidad inicial pero sin escalabilidad granular y con alto riesgo de fallos encadenados.

Criterios de elección:

Resiliencia: Una falla en módulos no críticos (reseñas, catálogo, etc.) no debe afectar la autenticación.

Escalabilidad: Capacidad de escalar operaciones de login/registro sin desplegar toda la plataforma.

Seguridad: Aislamiento de credenciales y datos sensibles en infraestructura controlada.

Decisión:
Se adopta el Patrón de Microservicio (Asignación Independiente en Contenedor) para el despliegue del SGU.

Sustento:
La arquitectura basada en microservicios exige independencia de ejecución y aislamiento de fallos entre servicios. Desplegar el SGU en su propio contenedor garantiza que operaciones críticas como autenticación se mantengan disponibles incluso durante fallos o mantenimientos de servicios secundarios. Adicionalmente, este patrón habilita la escalabilidad independiente del SGU en función de picos de login (ej. lanzamientos y eventos promocionales), sin incrementar innecesariamente los recursos del resto de módulos.

⬅️ Anterior
 | 🏠 Home
 | Siguiente ➡️