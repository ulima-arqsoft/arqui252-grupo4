8. Aplicación de Patrones Arquitecturales
 › 8.2. Módulo 2 / Integrante 2
 › 8.2.2. Patrones de Componente y Conector

8.2.2. Patrones de Componente y Conector
02: Módulo: Gestión de Usuarios

Título:
Autenticación basada en JWT + API REST como conector entre módulos (Patrón Solicitud–Respuesta)

Contexto:
El Servicio de Gestión de Usuarios (SGU) autentica a los usuarios y emite tokens JWT para permitir el acceso a los demás módulos de la plataforma. El mecanismo de conector entre los servicios debe ser seguro, escalable y desacoplado, evitando que cada microservicio dependa en tiempo real del SGU para validar credenciales.

Alternativas:

JWT + API REST (Patrón Solicitud–Respuesta):

El SGU valida credenciales y devuelve un token JWT firmado al cliente.

Los demás servicios verifican el token localmente sin consultar nuevamente al SGU.

Disminuye la latencia y elimina dependencias síncronas entre microservicios.

Sesiones Centralizadas (Stateful Session Store):

El SGU guarda sesiones en memoria o BD y los módulos deben consultarlo en cada operación.

Alto acoplamiento, cuellos de botella y mayor riesgo ante fallos.

OAuth2 / Proveedor OpenID Connect:

Opción robusta para federación de identidad empresarial.

Complejidad y alcance excesivo para la arquitectura actual del proyecto.

Criterios de elección:

Seguridad: Los tokens deben incluir expiración y firma criptográfica.

Rendimiento: Los demás servicios deben validar tokens sin depender del SGU.

Desacoplamiento: Cada módulo opera incluso si el SGU está temporalmente inactivo.

Decisión:
Se adopta el patrón de JWT + API REST para la autenticación y autorización entre microservicios.

Sustento:
JWT elimina dependencias directas con el SGU durante la ejecución. Una vez generado el token por AuthController, cada servicio valida la firma sin comunicación adicional con el SGU, reduciendo la latencia y aumentando la resiliencia del ecosistema. Este patrón refuerza la seguridad al encapsular la identidad del usuario y sus roles en un token de acceso firmado.

⬅️ Anterior
 | 🏠 Home
 | Siguiente ➡️