8. Aplicación de Patrones Arquitecturales
 › 8.2. Módulo 2 / Integrante 2
 › 8.2.1. Patrones de Módulo

8.2.1. Patrones de Módulo
02: Módulo: Gestión de Usuarios

Título:
Organización modular interna del Servicio de Gestión de Usuarios (SGU) (Patrón en Capas)

Contexto:
El Servicio de Gestión de Usuarios (Módulo 2) es el responsable de administrar la identidad digital de los usuarios: registro, inicio de sesión, recuperación de contraseñas, actualización de perfil y validación de roles/permisos. Para garantizar que estas funcionalidades críticas sean mantenibles y fáciles de probar, se requiere una estructura interna que separe claramente la lógica expuesta en API, la lógica de negocio y el acceso a datos.

Alternativas:

Patrón de Arquitectura en Capas (Layered Architecture):

Organización horizontal por capas técnicas: Controllers, Services, Repositories.

Aísla la lógica HTTP (API) de la lógica de negocio (validación de usuario, roles) y del módulo de persistencia (PostgreSQL).

Mejora la testabilidad al permitir simular los repositorios.

Arquitectura Hexagonal (Ports & Adapters):

Aísla completamente el dominio del SGU del resto del sistema mediante puertos y adaptadores.

Máximo desacoplamiento, pero mayor complejidad para este módulo.

Estructura Ad-hoc (Sin patrón):

Toda la lógica en un único archivo de handler.

Rápido para prototipos, pero con alto acoplamiento y baja mantenibilidad.

Criterios de elección:

Mantenibilidad: Facilidad para actualizar la política de roles y contraseñas sin afectar endpoints o persistencia.

Testabilidad: Capacidad de probar la lógica de autenticación “mockeando” únicamente UserRepository.

Cohesión: Encapsular las responsabilidades del SGU (RF-01 a RF-05) en un único servicio.

Decisión:
Se adopta el Patrón de Arquitectura en Capas para la estructura interna del Servicio de Gestión de Usuarios (SGU).

Sustento:
El patrón en capas está alineado directamente con nuestro Diagrama de Componentes (C4 Nivel 3). Permite separar AuthController y UserController (API), de AuthService y UserService (negocio), y UserRepository (persistencia). Esta separación es esencial para evolucionar políticas de autenticación y autorización sin riesgo de impactos colaterales. Adicionalmente, esta estructura facilita pruebas unitarias y el despliegue modular del SGU en producción.

🏠 Home
 | Siguiente ➡️