> [8. Aplicación de Patrones Arquitecturales](../../8.md) › [8.7. Listado Consolidado](../8.7.md) › [8.7.3. Patrones de Asignación](8.7.3.md)

# 8.7.3. Patrones de Asignación

## **01: Módulo: Carrito de Compras y Pedidos**

---

**Título:**
Asignación del `Servicio de Catálogo` en arquitectura Multi-Tier con aislamiento de datos

**Contexto:**
El `Servicio de Catálogo` (M1) es el componente con mayor tráfico de lectura del sistema (`ESC-01`). Requiere acceso a dos fuentes de datos: `PostgreSQL` (datos maestros) y `Elasticsearch` (búsqueda). La infraestructura debe garantizar que la carga pública de búsquedas no comprometa la seguridad de la base de datos maestra ni el rendimiento de otros servicios.

**Alternativas:**

  * **Patrón Multi-Tier (N-Capas):**

      * El `Servicio de Catálogo` se despliega en el **Tier de Aplicación** (Red Privada).
      * Las bases de datos (`PostgreSQL`, `Elasticsearch`) se despliegan en el **Tier de Datos** (Red Aislada).
      * **Ventaja:** Seguridad y escalabilidad independiente. Podemos escalar las réplicas de `Elasticsearch` en el Tier de Datos sin tocar el código de la aplicación.

  * **Colocación (Colocation):**

      * Desplegar el motor de búsqueda `Elasticsearch` en el mismo contenedor o pod que el servicio Node.js.
      * **Desventaja:** Dificulta el escalado horizontal (si escalo la app, escalo la BD innecesariamente). Riesgo de competencia por recursos (RAM).

**Criterios de elección:**

  * **Escalabilidad:** Capacidad de escalar el motor de búsqueda independientemente de la lógica de negocio.
  * **Seguridad:** Aislamiento de la base de datos maestra.

**Decisión:**
Se adopta el patrón **Multi-Tier**.

**Sustento:**
Permite cumplir con la restricción de uso de `Kubernetes` organizando los componentes en capas de red seguras. El tráfico fluye estrictamente: `Internet` -\> `Gateway (Tier 2)` -\> `Catálogo (Tier 3)` -\> `Datos (Tier 4)`.


## **03: Módulo: Carrito de Compras y Pedidos**

---

**Título:**
Asignación de componentes del `Servicio de Carrito y Pedidos (CCP)` en un Patrón Multi-Tier

**Contexto:**
El `Servicio de Carrito y Pedidos` (Módulo 3) es un componente transaccional que maneja datos sensibles (pedidos de clientes) y datos de alta volatilidad (carritos). Debemos decidir cómo se asignará este contenedor de software a la infraestructura de red en producción para garantizar la seguridad de los datos del pedido y el rendimiento del carrito.

**Alternativas:**

* **Patrón Multi-Tier (N-Capas):**
    * El `Servicio CCP` se asigna a un "Tier" de aplicación en una **red privada**.
    * Sus bases de datos (`PostgreSQL` para pedidos, `Redis` para carritos) se asignan a un "Tier" de datos en una **red aislada**.
    * Ventaja: Máxima seguridad. El acceso directo a la BD de pedidos desde Internet es imposible.
    * Ventaja: Permite escalar el servicio de CCP independientemente de sus bases de datos.

* **Despliegue en un Solo Tier (Flat Deployment):**
    * Asignar el `Servicio CCP` y sus bases de datos (`PostgreSQL`, `Redis`) a la misma red donde reside el `API Gateway` (expuesto a Internet).
    * Ventaja: Configuración de red más simple.
    * Desventaja: Brecha de seguridad grave. Una vulnerabilidad en el Gateway podría exponer directamente la BD de pedidos y la caché de carritos.

**Criterios de elección:**

* **Seguridad:** Aislamiento de la base de datos de pedidos (datos sensibles) del acceso público.
* **Rendimiento:** Asegurar la conectividad de baja latencia entre el `CCP` y su caché `Redis`.
* **Cumplimiento de Restricciones:** Alineación con la restricción de usar `Kubernetes`, que facilita la segmentación de red.

**Decisión:**
Se adopta el patrón **Multi-Tier** para el despliegue del `Servicio de Carrito y Pedidos (CCP)` y sus bases de datos.

**Sustento:**
Este patrón es la única alternativa que cumple con los requisitos de seguridad para los datos de pedidos. Al asignar el `Servicio CCP` al **Tier de Aplicación (Red Privada)** y sus bases de datos (`PostgreSQL` y `Redis`) al **Tier de Datos (Red Aislada)**, garantizamos que un atacante no pueda acceder directamente a la información. El `API Gateway` (Tier 2) filtra el tráfico antes de que llegue al `CCP` (Tier 3).

## **04: Módulo: Sistema de Pagos**

---

**Título:**
Asignación de componentes del Servicio de Pagos (SP) en un Patrón Multi-Tier

**Contexto:**
El Servicio de Pagos (Módulo 4) es un componente crítico que maneja información financiera y se comunica con un socio externo (Culqi). Debemos decidir cómo se asignará este contenedor de software a la infraestructura de red en producción para garantizar la máxima seguridad y cumplir con los estándares de pago (PCI-DSS).

**Alternativas:**

* **Patrón Multi-Tier (N-Capas):**
    * El Servicio de Pagos (SP) se asigna a un "Tier" de aplicación en una red privada, aislado de Internet.
    * La Base de Datos de Pagos (PostgreSQL) se asigna a un "Tier" de datos en una red aislada, inaccesible excepto desde el Tier de aplicación.
    * Ventaja: Máxima seguridad. El acceso directo a la BD de pagos desde Internet es imposible.

* **Despliegue en un Solo Tier (Flat Deployment):**
    * Asignar el Servicio de Pagos (SP) y su Base de Datos (PostgreSQL) a la misma red donde reside el API Gateway (expuesto a Internet).
    * Ventaja: Configuración de red más simple.
    * Desventaja: Brecha de seguridad grave. Una vulnerabilidad en el Gateway podría exponer directamente la BD de transacciones.

**Criterios de elección:**

* **Seguridad (PCI-DSS):** Cumplir con la restricción de que los datos financieros sensibles deben estar protegidos y aislados.
* **Aislamiento:** Asegurar que el Servicio de Pagos solo reciba tráfico legítimo desde el API Gateway y el Servicio de Pedidos.
* **Cumplimiento de Restricciones:** Alineación con la restricción de usar Kubernetes, que facilita este tipo de segmentación de red.

**Decisión:**
Se adopta el patrón Multi-Tier para el despliegue del Servicio de Pagos (SP) y su base de datos.

**Sustento:**
Este patrón es la única alternativa que cumple con los requisitos de seguridad para el Módulo 4. Al asignar el Servicio de Pagos (SP) al Tier de Aplicación (Red Privada) y su base de datos PostgreSQL al Tier de Datos (Red Aislada), garantizamos que un atacante no pueda acceder directamente a la información de transacciones. El API Gateway (Tier 2) actúa como un "guardia de seguridad" que filtra el tráfico antes de que llegue al SP (Tier 3).    


## **05: Módulo: Comunidad y Reseñas**

---

**Título:**
Patrón de Microservicio (Asignación en Contenedor Independiente)

**Contexto:**
El `Servicio de Comunidad y Reseñas (SCR)` (Módulo 05) debe ser asignado a la infraestructura de ejecución. Este módulo tiene un perfil de carga muy diferente al resto (alta lectura, baja escritura). Se debe decidir si desplegar este componente de forma aislada o como parte de un despliegue más grande.

**Alternativas:**

* **Patrón de Microservicio (Asignación Independiente en Contenedor):**
    * El `SCR` se empaqueta y despliega como un servicio separado (ej. Docker/Kubernetes).
    * Ventaja: **Escalabilidad Granular** (escalar solo Reseñas) y **Aislamiento de Fallos** (un bug en `SCR` no afecta los Pagos).
    * Desventaja: Mayor complejidad de red y gestión de servicios.

* **Patrón Monolítico (Asignación Multi-Tier Clásica):**
    * Se despliega *toda* la aplicación (Reseñas + Pagos + Carrito) como un solo bloque lógico (el "Monolito") en un **Nivel de Aplicación** (Tier 2).
    * Este nivel se comunica con un **Nivel de Base de Datos** (Tier 3) separado.
    * Ventaja: Más estructurado y seguro que un monolito unificado simple (separación de BD y App).
    * Desventaja: Sigue teniendo escalabilidad "todo o nada" y cero aislamiento de fallos *dentro* del Nivel de Aplicación.

* **Patrón Monolítico (Asignación Unificada Simple):**
    * Desplegar *toda* la aplicación (el código) y la base de datos en el *mismo servidor* o máquina virtual (Tier 1).
    * Ventaja: Máxima simplicidad para prototipos o desarrollo.
    * Desventaja: Cero escalabilidad, cero resiliencia, alto riesgo de seguridad.

**Criterios de elección:**

* **Resiliencia (Aislamiento de Fallos):** La falla de un módulo no crítico (Reseñas) no debe afectar a módulos críticos (Pagos, Carrito).
* **Escalabilidad Granular:** Capacidad de escalar servicios individualmente.
* **Mantenibilidad:** Poder actualizar el SCR sin redesplegar toda la plataforma.

**Decisión:**
Se adopta el **Patrón de Microservicio (Asignación Independiente en Contenedor)**.

**Sustento:**
Se descartan las dos alternativas monolíticas. La **Asignación Unificada Simple** no es viable para producción. La **Asignación Multi-Tier** es mejor, pero aún viola el criterio de Resiliencia: un bug en el código de Reseñas colapsaría todo el Nivel de Aplicación, deteniendo las ventas. El **Patrón de Microservicio** es el único que garantiza el aislamiento de fallos y la escalabilidad granular que este módulo (con su perfil de carga único) requiere.


## 06: Módulo: Análisis de Datos y Reportes

---

**Título:**
Procesamiento de datos masivos mediante **Patrón Map-Reduce**

**Contexto:**
El `Servicio de Análisis (ADR)` tiene la responsabilidad de procesar grandes volúmenes de eventos generados por la plataforma (ventas, registros, actividades) para generar reportes históricos y tendencias (`RF-02`, `RF-03`). El procesamiento secuencial tradicional en un solo servidor no es eficiente para analizar grandes conjuntos de datos (Dataset) y puede generar cuellos de botella. [cite\_start]Se requiere una estrategia de asignación que permita el procesamiento paralelo y eficiente[cite: 816].

**Alternativas:**

  * **Patrón Map-Reduce:** [cite: 812]

      * **Descripción:** Un patrón de asignación diseñado para analizar grandes volúmenes de datos. Divide la tarea en dos funciones distribuidas:
          * **Map:** Filtra y ordena los datos (ej. filtrar eventos de "Venta" por región).
          * **Reduce:** Consolida los resultados (ej. sumar el total de ventas por región).
      * **Ventaja:** Procesamiento paralelo y eficiente de datos relacionados[cite: 817].
      * **Ventaja:** Alta disponibilidad y escalabilidad al distribuir la carga en nodos de procesamiento[cite: 829].

  * **Procesamiento Monolítico Secuencial:**

      * **Descripción:** Un solo proceso lee todos los registros uno por uno, calcula y guarda.
      * **Desventaja:** Si el volumen de datos crece (ej. Black Friday), el tiempo de procesamiento aumenta linealmente o exponencialmente, incumpliendo los SLA de reportes.

**Criterios de elección:**

  * **Eficiencia de Procesamiento:** Capacidad de analizar grandes volúmenes de datos rápidamente (`ESC-20`).
  * **Escalabilidad:** Capacidad de distribuir la carga de trabajo de análisis.

**Decisión:**
Se adopta el patrón **Map-Reduce** para la asignación de tareas de análisis.

**Sustento:**
Este patrón es la solución estándar académica para problemas de análisis de datos masivos (`Big Data`) presentada en el curso[cite: 814]. Se alinea con la arquitectura del Módulo 6, donde la función **Map** corresponde a la ingesta y filtrado de eventos de Kafka, y la función **Reduce** corresponde a la agregación de datos (sumas, promedios) realizada por los scripts de Python/Pandas antes de guardar en el Data Warehouse.

---

[⬅️ Anterior](../8.7.2/8.7.2.md) | [🏠 Home](../../../README.md) | [Siguiente ➡️](../8.7.4/8.7.4.md)
